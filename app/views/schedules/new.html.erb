<div class='well'>
  <%= form_for @schedule do |f| %>

    <div class='control-group'>
      <%= f.label :shift %>
      <%= f.text_field :shift %>
    </div>
    <%= f.submit @schedule.new_record? ? "Add Shift" : "Update", class: 'btn btn-success' %>
  <% end %>
</div>