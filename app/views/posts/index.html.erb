<h3>Posts</h3>

<div class="well">
<% @posts.each do |post| %>
  <th>
    <dt><%= post.title %></dt>
    <dt><%= post.description %></dt>
    <dt><p>Created at <%= post.created_at %></p></dt>
    <%= link_to "Show", employee_path(@employee) %>
  </th>
<% end %>
</div>

